// Imports
#import "@preview/brilliant-cv:4.1.0": cv-section, cv-skill, h-bar


#cv-section("Skills")

#cv-skill(
  type-width: 17.5%,
  type: [Languages],
  info: [German (Mother tongue) #h-bar() English (Fluent, C1) #h-bar() French (Proficient, C1) #h-bar() Spanish (Basic, A2)],
)

#cv-skill(
  type-width: 17.5%,
  type: [Programming],
  info: [Git (GitLab, GitHub) #h-bar() LaTeX #h-bar() Power Query (M) \& DAX #h-bar() Python (pandas, NumPy) #h-bar() SQL (DB2, Impala, PostgreSQL) #h-bar() VBA #h-bar() R #h-bar() Stata],
)

#cv-skill(
  type-width: 17.5%,
  type: [ML / Analytics],
  info: [ML (scikit-learn, PyTorch) #h-bar() NLP (spaCy, NLTK) #h-bar() Econometrics (Synthetic DiD, IVs, matching)],
)

#cv-skill(
  type-width: 17.5%,
  type: [Domain / Regulatory],
  info: [Banking Supervision (SREP) #h-bar() Basel/CRR (RWA, CET1) #h-bar() Financial Reporting (FINREP, HGB) #h-bar() IT Risk],
)

#cv-skill(
  type-width: 17.5%,
  type: [Visualization \& Tools],
  info: [Dash Plotly #h-bar() Power BI #h-bar() Tableau #h-bar() Excel #h-bar() BPMN #h-bar() KoboToolbox #h-bar() UNIX/Linux #h-bar() Prefect #h-bar() Azure ML #h-bar() MLflow #h-bar() Docker],
)
